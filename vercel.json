{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@now/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ],
    "routes": [
        { "src": "/storybook/(.*)", "dest": "/storybook/$1" },
        { "src": "/(?!res)(.*)", "dest": "/styleguide/$1" }
    ]
}
